# CodeAlpha_DiabetesPredictor
This project is a Machine Learning–based web app that predicts the likelihood of diabetes using patient health data. It applies data preprocessing, feature selection, and model tuning techniques to train and evaluate multiple classifiers. The best-performing model is integrated into a user-friendly Streamlit interface for real-time predictions.
